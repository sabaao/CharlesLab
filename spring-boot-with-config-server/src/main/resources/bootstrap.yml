spring:
  application:
    name: didispace
  profiles:
    active: dev
  cloud:
    config:
      #uri: http://localhost:7001
      discovery:
        enabled: true
        serviceId: config-server
server:
  port: 7002
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/